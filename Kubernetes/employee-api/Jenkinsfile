pipeline {
    agent any

    stages {
        stage('Environment Check') {
            steps {
                bat 'whoami'
                bat 'docker --version'
                bat 'docker ps'
                bat 'kubectl get nodes'
                bat 'git --version'
            }
        }
    }
}