apiVersion: v1
kind: Pod

metadata:
  name: multi-container-pod

spec:
  containers:

  - name: web
    image: nginx

  - name: logger
    image: busybox
    command: ["sh", "-c", "while true; do echo Logging...; sleep 5; done"]